fastapi>=0.115
uvicorn>=0.30
pytest>=8.0
httpx>=0.27
# A-2b (required for LightGBM GBDT heads). The trainer auto-detects
# availability: if lightgbm is missing, `--model-kind=auto` silently
# falls back to sklearn linear. Install lightgbm + scikit-learn on any box
# that produces models; the C++ inference runtime does NOT depend on them.
lightgbm>=4.0
scikit-learn>=1.3

# B-2a optional ML dependencies. Marked optional via code-level import checks
# so production deployments that don't need the neural policy don't have to
# carry them. Install with: pip install -r backend/requirements.txt
torch>=2.0
onnx>=1.15
onnxruntime>=1.17
onnxscript>=0.1

